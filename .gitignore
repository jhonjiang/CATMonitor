bin/
docker/.build/
# 二进制产物仅在 bin/ 下；不要用裸 "catmonitor" 规则，会误伤 cmd/catmonitor 源码目录
bin/catmonitor
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
.idea/
.vscode/
features/web/data/
features/web/.runtime-*.tmp
**/ipmi_sensor_map.json

# AGENTS.md is local-only, do not sync to remote
AGENTS.md

# local_docs is local-only, do not sync to remote
local_docs/

# loc_configs is local-only, do not sync to remote
local_configs/
